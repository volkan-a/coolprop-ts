{
  "name": "coolprop-ts",
  "version": "1.0.0",
  "description": "CoolProp npm package",
  "main": "index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volkan-a/coolprop-ts.git"
  },
  "keywords": [
    "coolprop",
    "thermodynamics"
  ],
  "author": "Volkan Akkaya vakkaya@gmail.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/volkan-a/coolprop-ts/issues"
  },
  "homepage": "https://github.com/volkan-a/coolprop-ts#readme",
  "devDependencies": {
    "prettier": "^2.1.2",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.0.5"
  }
}
